export type SearchParams = {
  isOpen: boolean;
  query: string;
};

export type AdvancedSearchParams = {
  distance: number;
  genre: string;
  isOpen: boolean;
  keyword: string;
  place: string;
};
